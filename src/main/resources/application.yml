spring:
  datasource:
    url: jdbc:mysql://localhost:3306/your_database
    username: root
    password: admin1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQL8Dialect # MySQL 8.x에 대한 Hibernate 방언

aws:
  dynamodb:
    endpoint: http://localhost:8001
    region: ap-northeast-2

